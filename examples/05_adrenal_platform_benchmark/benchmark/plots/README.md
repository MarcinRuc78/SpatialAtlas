# Generated benchmark plots

Run `plot_operational_benchmark.py` and `plot_independent_viewer_benchmark.py` from the repository root after extracting the required release assets. PNG and SVG outputs are written to this directory and are intentionally not committed.
